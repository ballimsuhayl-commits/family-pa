{
  "name": "rosie-brain-worker",
  "private": true,
  "version": "15.0.0",
  "description": "Rosie Brain Cloudflare Worker (WhatsApp inbound + bridge API)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}