name = "rosie-brain"
main = "worker.js"
compatibility_date = "2025-01-01"

# Bindings (set in Cloudflare Dashboard or via `wrangler kv namespace create`)
# KV namespace binding:
# kv_namespaces = [
#   { binding = "INBOX_KV", id = "YOUR_KV_NAMESPACE_ID" }
# ]
