name = "rosie-brain-worker"
main = "src/index.js"
compatibility_date = "2025-01-01"

# Enable a cron to send reminders (optional)
[triggers]
crons = ["*/5 * * * *"]

# KV namespace for message storage (create in Cloudflare dashboard and paste id)
# [[kv_namespaces]]
# binding = "ROSIE_KV"
# id = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
