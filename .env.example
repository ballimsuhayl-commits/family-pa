# Frontend: no secrets required. Firebase web config is PUBLIC and is pasted in-app.
# Backend: Gemini key is stored as a Firebase Secret (recommended)
# Set it with:
#   firebase functions:secrets:set GEMINI_API_KEY
